{
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy --config wrangler.toml",
    "tan-test": "node scripts/tan-test.mjs",
    "check": "node --check worker.js && node --check scripts/run-tanpheus.mjs && node scripts/self-check.mjs && node scripts/worker-self-check.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.28.1"
  }
}
